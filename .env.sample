# Optional: server-side DashScope / Model Studio API key.
# If set, browser clients can start a session without pasting a key
# (a key pasted in the page takes priority when provided).
# Create one at https://bailian.console.aliyun.com/ (mainland)
# or https://modelstudio.console.alibabacloud.com/ (Singapore/Intl).
DASHSCOPE_API_KEY=
